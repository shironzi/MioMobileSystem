import { View, Text } from "react-native";
import React, { memo } from "react";

const calendar = () => {
  return (
    <View>
      <Text>calendar</Text>
    </View>
  );
};

export default memo(calendar);

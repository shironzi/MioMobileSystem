import { View, Text } from "react-native";
import React, { memo } from "react";

const profile = () => {
  return (
    <View>
      <Text>profile</Text>
    </View>
  );
};

export default memo(profile);

import { View, Text } from "react-native";
import React, { memo } from "react";

const specializedDetails = () => {
  return (
    <View>
      <Text>specializedDetails</Text>
    </View>
  );
};

export default memo(specializedDetails);
